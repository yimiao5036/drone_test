# 项目文件结构与命名规划

## 1. 模块边界

正式工程按业务职责拆分，视频链路不属于YOLO模块：

- `video`：视频接收、解码、帧内存池、叠加、编码和推送。
- `perception`：YOLO、RGA模型预处理、检测后处理及其他感知算法。
- `communication`：PX4串口、MAVLink、电台和地面站通信。
- `control`：轨迹预测和PX4控制命令生成。
- `state_machine`：任务状态与控制权管理。
- `common`：发布订阅、公共消息类型、时间和通用基础设施。
- `config`：配置解析与参数校验。

根目录中的 `videoPart` 暂时作为硬件验证原型保留。正式模块验证完成后，再按本文映射迁移；迁移前不删除原型，避免丢失已验证的RKNN、RGA和rkmpp调用方式。

## 2. 当前创建的正式工程结构

```text
drone_test/
├── CMakeLists.txt                # 7 个模块静态库 + drone_control 可执行 + 测试
├── main.cpp                      # 唯一正式程序入口，只组合和启动模块
├── README.md
├── docs/                         # 全部设计文档（含 开发思路.md、状态机设计.md）
│   └── 数据接口文档.md            # 模块间代码级接口契约（消息类型/接口签名/Topic 一览）
├── include/
│   ├── common/
│   │   ├── topic.h               # 已完成：类型安全的有界发布订阅主题
│   │   ├── logger.h              # 已完成：异步文件日志（等级可配置）
│   │   └── types.h               # 已完成：公共消息类型 + 枚举 + Topic 名称常量
│   ├── video/
│   │   ├── video_frame.h         # 已完成：帧格式、stride、时间戳、RAII 缓冲句柄
│   │   ├── video_frame_pool.h    # 已完成：固定容量内存池
│   │   ├── camera_receiver.h     # 接口抽象：ICameraReceiver + CameraReceiverStub
│   │   └── video_decoder.h       # 接口抽象：IVideoDecoder + VideoDecoderStub
│   ├── video_transmission/
│   │   └── video_sender.h        # 接口抽象：IVideoSender + VideoSenderStub
│   ├── perception/
│   │   ├── yolo_detector.h       # 接口抽象：IYoloDetector + YoloDetectorStub
│   │   ├── optical_flow_estimator.h
│   │   ├── laser_range_finder.h
│   │   ├── perception_fusion.h
│   │   └── target_estimator.h
│   ├── communication/
│   │   ├── serial_port.h         # 已完成：串口封装（termios 真实实现）
│   │   ├── px4_link.h            # 接口抽象：IPx4Link + Px4LinkStub
│   │   └── ground_station_link.h # 接口抽象：IGroundStationLink + GroundStationLinkStub
│   ├── control/
│   │   └── flight_controller.h   # 接口抽象：IFlightController + FlightControllerStub
│   ├── state_machine/
│   │   └── mission_state_machine.h
│   ├── health/
│   │   └── health_manager.h      # 接口抽象：IHealthManager + HealthManagerStub
│   └── config/                   # 配置接口
├── src/
│   ├── common/                   # logger.cpp
│   ├── video/                    # video_frame_pool.cpp + 两个 Stub 实现
│   ├── video_transmission/
│   ├── perception/
│   ├── communication/            # serial_port.cpp（真实实现）+ 两个 Stub
│   ├── control/
│   ├── state_machine/
│   └── health/
├── tests/
│   ├── common/                   # topic_test.cpp
│   ├── video/                    # video_frame_test.cpp, video_frame_pool_test.cpp
│   ├── skeleton/                 # stub_smoke_test.cpp（骨架冒烟测试）
│   └── ...                       # 其余模块目录待业务逻辑阶段补充
├── config/                       # JSON 运行配置（config.json：日志目录/等级）
├── third_party/                  # spdlog / mavlink / nlohmann
└── videoPart/                    # 现有原型，后续迁移
```

> 接口抽象说明：所有部件接口以 `I` 前缀命名（如 `ICameraReceiver`），骨架期提供
> `XxxStub` 占位实现（带生命周期与未实现节流日志）；接口签名在 `docs/数据接口文档.md`
> §4 冻结，业务逻辑阶段只替换 Stub 实现、不改接口。

## 3. 后续文件命名

### 3.1 common

```text
include/common/
├── topic.h                          # Topic<T>发布订阅
├── types.h                          # 跨模块公共消息类型
└── clock.h                          # 单调时钟抽象，便于测试
```

不创建一个包含所有功能的“工具类”。公共类型、时间和消息基础设施分别维护。

### 3.2 video

```text
include/video/
├── video_frame.h                    # 帧元数据和RAII缓冲句柄
├── video_frame_pool.h               # 固定容量视频内存池
├── video_source.h                   # 视频输入抽象接口
├── video_sink.h                     # 视频输出抽象接口
├── rtsp_receiver.h                  # RTSP接收与解封装
├── rkmpp_decoder.h                  # MPP硬件解码
├── frame_compositor.h               # 检测框等视频叠加
├── rkmpp_encoder.h                  # MPP硬件编码
└── rtsp_publisher.h                 # RTSP推送

src/video/
├── video_frame_pool.cpp
├── rtsp_receiver.cpp
├── rkmpp_decoder.cpp
├── frame_compositor.cpp
├── rkmpp_encoder.cpp
└── rtsp_publisher.cpp
```

接收、解码、编码和网络推送分别封装，避免将网络阻塞、硬件编解码和YOLO放进同一个类。

### 3.3 perception

```text
include/perception/
├── detector.h                       # 目标检测抽象接口
├── yolo_detector.h                  # YOLO26检测器
├── rga_preprocessor.h               # NV12/RGB模型预处理
└── yolo_postprocessor.h             # 输出解码、阈值过滤和NMS

src/perception/
├── yolo_detector.cpp
├── rga_preprocessor.cpp
└── yolo_postprocessor.cpp
```

YOLO模块消费视频帧并发布结构化检测结果，不建立RTSP连接，也不负责视频编码与推送。

### 3.4 communication、control和state_machine

```text
include/communication/
├── serial_port.h
├── mavlink_handler.h
├── px4_link.h
└── radio_link.h

include/control/
├── trajectory_predictor.h
└── px4_controller.h

include/state_machine/
└── state_machine.h

include/config/
└── config.h
```

具体源文件放入对应的 `src/<module>/`，测试放入 `tests/<module>/`。

## 4. 原型文件迁移映射

| 当前原型 | 正式文件 | 说明 |
|----------|----------|------|
| `rtsp_decoder.cpp`中的RTSP读取 | `video/rtsp_receiver.cpp` | 只负责连接、解封装和压缩包输出 |
| `rtsp_decoder.cpp`中的rkmpp解码 | `video/rkmpp_decoder.cpp` | 只负责压缩包到视频帧 |
| `rtsp_encoder.cpp`中的颜色转换/编码 | `video/rkmpp_encoder.cpp` | 编码职责独立 |
| `rtsp_encoder.cpp`中的RTSP写包 | `video/rtsp_publisher.cpp` | 网络输出故障不能影响输入和感知 |
| `DrawDetections()` | `video/frame_compositor.cpp` | 使用检测结果叠加视频 |
| `yolo26_detector.*` | `perception/yolo_detector.*` | YOLO对外检测接口 |
| `rga_utils.*`中的模型预处理 | `perception/rga_preprocessor.*` | 只保留模型输入相关RGA操作 |
| `rknn_model.cpp`后处理 | `perception/yolo_postprocessor.cpp` | 输出布局转换、过滤与NMS |
| 原型 `src/main.cpp` | 根目录 `main.cpp` | 根入口只负责模块装配和生命周期 |

## 5. 发布订阅约定

当前首先提供 `drone::common::Topic<Message>`：

- 消息类型在编译期确定，禁止 `void*` 和无类型字节指针。
- 每个订阅者拥有独立有界队列。
- 默认队列满时丢弃最旧消息，适合视频帧和无人机最新状态。
- 可选择拒绝最新消息，用于不能覆盖旧消息的场景。
- 消息通过 `std::shared_ptr<const Message>` 分发，多订阅者间不复制消息对象。
- `Subscription`使用RAII管理订阅生命周期，析构后自动取消订阅。
- 慢订阅者不会在发布者线程执行回调，也不会阻塞其他订阅者。
- 主题关闭会唤醒等待线程，支持确定性停机。

示例：

```cpp
struct DroneState {
    std::uint64_t sequence = 0;
    double latitude = 0.0;
    double longitude = 0.0;
};

drone::common::Topic<DroneState> drone_state_topic;
auto controller = drone_state_topic.Subscribe(1);
auto logger = drone_state_topic.Subscribe(32);

auto state = std::make_shared<const DroneState>(DroneState{1, 30.0, 120.0});
drone_state_topic.Publish(state);
```

视频帧主题应发布内存池返回的RAII帧句柄。多个订阅者只共享句柄；底层缓冲在最后一个消费者释放句柄时自动归还内存池。

## 6. 内存与资源管理规则

- 业务代码不使用 `malloc`、`calloc`、`realloc`、`free`、裸 `new` 或裸 `delete`。
- 独占资源使用 `std::unique_ptr`，共享不可变消息使用 `std::shared_ptr<const T>`。
- FFmpeg、MPP、RGA、RKNN等C接口必须使用带自定义deleter的智能指针或独立RAII包装类。
- 不把C接口返回的裸句柄直接暴露到模块外。
- 视频缓冲使用固定容量内存池，不在逐帧热路径中反复申请大块内存。
- 跨线程消息默认不可变；确需修改时创建新的所有权明确的输出帧。
- 构造和初始化失败可以抛异常；运行期单帧失败使用状态码或 `std::optional`。
- 容器和队列必须有容量上限，并记录丢弃计数和最大深度。

## 7. 下一步实施顺序

1. 定义 `video_frame.h` 的格式、stride、时间戳和RAII缓冲句柄。 ✅ 已完成
2. 实现固定容量 `VideoFramePool`，明确池耗尽时的丢帧策略。 ✅ 已完成
3. 将RTSP接收和MPP解码迁入 `video`，发布解码帧主题（替换 `ICameraReceiver`/`IVideoDecoder` 的 Stub）。
4. 将YOLO迁入 `perception`，订阅解码帧并发布检测结果（替换 `IYoloDetector` 的 Stub）。
5. 实现视频叠加、MPP编码和RTSP输出，订阅帧与检测结果（替换 `IVideoSender` 的 Stub）。
6. 接入PX4、地面站和无人机状态主题（替换 `IPx4Link`/`IGroundStationLink`/`IMissionStateMachine`/`IFlightController`/`IHealthManager` 的 Stub）。
7. 根目录 `main.cpp` 完成依赖装配、线程启动和逆序停机。

> 第 0 步（已完成）：各部件接口抽象与数据接口文档先行，接口签名以 `docs/数据接口文档.md` 为准。
