# 双目硬件事实确认（探测脚本 + 回填模板）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付板上 v4l2 探测脚本与事实确认记录模板，用户上板探测后把结果回填进《双目深度估计思路.md》与《开发进度.md》，关闭 P1~P4 待确认项。

**Architecture:** 纯增量交付：1 个 bash 诊断脚本（`tools/`，不进构建）+ 1 个记录模板（`docs/`）。脚本在香橙派上枚举 UVC 节点、对拍格式档位、逐档实测帧率、确认 buffer 时间戳来源、抓帧存图，全部汇入报告目录；分析会话据报告与抓帧填写模板并回填设计文档。

**Tech Stack:** bash（v4l2-ctl）、python3（板上拆分 MJPG 抓帧，可选 ffmpeg 兜底）、Markdown、git。

**Spec:** `docs/superpowers/specs/2026-09-21-双目硬件事实确认-design.md`（脚本与模板的完整内容定义的权威来源；本计划按其逐字落地）。

## Global Constraints

- 沟通、注释、提交说明、文档统一中文；标识符与命令行保留英文。
- 提交说明末尾必须带 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。
- `.gitattributes` 强制 LF 行尾；脚本与文档一律 LF，禁止批量转换行尾。
- 不改 `src/`、`include/`、`config/`；不进 CMake 构建；不影响 `ctest` 基线（206/206）。
- 分支：`biao_dev`（当前分支，直接在其上提交）。
- 脚本依赖仅 `v4l-utils`；python3 / ffmpeg 为可选拆分工具。
- 报告与抓帧输出目录（`stereo_probe_*/`）不入 git。

---

### Task 1: 探测脚本落盘与本地校验

**Files:**
- Create: `tools/stereo_camera_probe.sh`

**Interfaces:**
- Produces: `tools/stereo_camera_probe.sh`，用法 `bash tools/stereo_camera_probe.sh [输出根目录]`，环境变量 `DEV=/dev/videoN` 可指定节点。输出 `stereo_probe_YYYYMMDD_HHMMSS/report.txt`（分节：0 环境 / 1 设备枚举 / 2 节点详情 / 3 格式档位 / 4 实测帧率 / 5 时间戳来源 / 6 抓帧 / 7 完成）与 `frames/<WxH>_NN.jpg`。Task 3 在板上运行它；Task 4 消费其报告结构。

- [ ] **Step 1: 创建脚本文件**

将以下内容**逐字**写入 `tools/stereo_camera_probe.sh`（与 spec §5 一致）：

````bash
#!/usr/bin/env bash
# stereo_camera_probe.sh —— 双目 UVC 相机硬件事实确认探测脚本（香橙派 RK3588 板上运行）
#
# 用途：为《双目深度估计思路.md》P3/P4 待确认项做板上实证：
#   1) 枚举 UVC 采集节点（SBS 同帧单流应仅 1 个；双流会出 2 个）
#   2) 列出全部格式/分辨率/帧间隔，与规格书对拍
#   3) 逐档实测 MJPG 帧率（USB2.0 带宽验证）
#   4) 确认 buffer 时间戳来源（印证设计文档 §3.3）
#   5) 目标档位抓帧存图（SBS 判读 + 画质检查）
#
# 依赖：v4l-utils 与 usbutils（sudo apt install -y v4l-utils usbutils）；python3 或 ffmpeg 可选（用于拆分抓帧）
# 用法：bash tools/stereo_camera_probe.sh [输出根目录=.]
#       可用 DEV=/dev/videoN 环境变量指定探测节点（默认取第一个采集节点）
# 注意：运行期间确保没有其他进程占用相机节点；脚本每步失败仅记录不中断。

set -u

OUT_ROOT="${1:-.}"
TS="$(date +%Y%m%d_%H%M%S)"
OUT_DIR="${OUT_ROOT}/stereo_probe_${TS}"
FRAMES_DIR="${OUT_DIR}/frames"
REPORT="${OUT_DIR}/report.txt"

log() { echo "$*" | tee -a "${REPORT}"; }
section() {
    log ""
    log "================================================================"
    log "== $1"
    log "================================================================"
}
# 执行命令并把输出写入报告；失败记录后继续
run() {
    log "\$ $*"
    "$@" >>"${REPORT}" 2>&1
    local rc=$?
    if [ "${rc}" -ne 0 ]; then
        log "!! 命令失败 rc=${rc}: $*（已记录，继续后续步骤）"
    fi
    return "${rc}"
}
# 判断档位是否存在于设备列表
have_res() {
    local r="$1" x
    for x in "${RES_LIST[@]:-}"; do
        [ "$x" = "$r" ] && return 0
    done
    return 1
}

mkdir -p "${FRAMES_DIR}" || { echo "无法创建输出目录: ${OUT_DIR}" >&2; exit 1; }

section "0. 环境与自检"
log "探测时间 : $(date '+%F %T')"
log "主机内核 : $(uname -a)"
if ! command -v v4l2-ctl >/dev/null 2>&1; then
    log "缺少 v4l2-ctl。请先执行: sudo apt update && sudo apt install -y v4l-utils"
    exit 1
fi
log "v4l2-ctl  : $(v4l2-ctl --version 2>&1 | head -n 1)"
run lsusb
run lsusb -t
# dmesg 可能受限（kernel.dmesg_restrict），尽力而为
if dmesg_out="$(sudo -n dmesg 2>/dev/null || dmesg 2>/dev/null)"; then
    log "\$ dmesg | grep -iE 'uvc|video|usb' | tail -n 40"
    echo "${dmesg_out}" | grep -iE 'uvc|video|usb' | tail -n 40 >>"${REPORT}"
else
    log "!! dmesg 不可读（需 root），跳过内核日志摘录"
fi

section "1. 设备枚举（P3 判据一：采集节点数）"
run v4l2-ctl --list-devices
NODES=()
for n in /dev/video*; do
    [ -e "$n" ] || continue
    caps="$(v4l2-ctl -d "$n" --all 2>/dev/null | awk -F: '/Device Caps/{print $2}')"
    if echo "${caps}" | grep -q "Video Capture" && ! echo "${caps}" | grep -q "Multiplanar"; then
        NODES+=("$n")
        log "采集节点: $n  (Device Caps:${caps})"
    fi
done
if [ "${#NODES[@]}" -eq 0 ]; then
    log "!! 未发现任何具备 Video Capture 能力的节点，相机未识别？请检查 USB 连接后重跑"
    exit 1
fi
log "采集节点共 ${#NODES[@]} 个: ${NODES[*]}"
log "判读：1 个 => SBS 同帧单流；2 个 => 可能双流（以第 6 节抓帧判读为准）"

DEV="${DEV:-${NODES[0]}}"
log "探测节点 DEV=${DEV}"

section "2. 节点详细信息"
run v4l2-ctl -d "${DEV}" --all

section "3. 格式与档位（与规格书对拍）"
run v4l2-ctl -d "${DEV}" --list-formats-ext
run v4l2-ctl -d "${DEV}" --get-parm

# 提取 MJPG 档位（分辨率列表）
mapfile -t RES_LIST < <(
    v4l2-ctl -d "${DEV}" --list-formats-ext 2>/dev/null |
    awk '/^[[:space:]]*\[/{in_mjpg=($0 ~ /MJPG|Motion-JPEG/)} /Size: Discrete/ && in_mjpg {print $3}' |
    sort -u
)
if [ "${#RES_LIST[@]}" -eq 0 ]; then
    log "!! 未解析到 MJPG 档位，跳过后续实测与抓帧"
    section "7. 完成"
    log "报告目录: ${OUT_DIR}"
    exit 1
fi
log "MJPG 档位: ${RES_LIST[*]}"

section "4. MJPG 各档实测帧率（每档 300 帧）"
for res in "${RES_LIST[@]}"; do
    w="${res%x*}"; h="${res#*x}"
    log "---- ${w}x${h} ----"
    run v4l2-ctl -d "${DEV}" "--set-fmt-video=width=${w},height=${h},pixelformat=MJPG"
    run v4l2-ctl -d "${DEV}" --get-fmt-video
    log "\$ v4l2-ctl -d ${DEV} --stream-mmap=4 --stream-count=300"
    timeout 40 v4l2-ctl -d "${DEV}" --stream-mmap=4 --stream-count=300 >>"${REPORT}" 2>&1
    rc=$?
    [ "${rc}" -ne 0 ] && log "!! 档位 ${w}x${h} 实测失败 rc=${rc}"
done

section "5. buffer 时间戳来源（印证设计文档 §3.3）"
TS_RES="3840x1080"
have_res "${TS_RES}" || TS_RES="${RES_LIST[0]}"
w="${TS_RES%x*}"; h="${TS_RES#*x}"
run v4l2-ctl -d "${DEV}" "--set-fmt-video=width=${w},height=${h},pixelformat=MJPG"
log "\$ v4l2-ctl -d ${DEV} --stream-mmap=4 --stream-count=3 --verbose"
timeout 20 v4l2-ctl -d "${DEV}" --stream-mmap=4 --stream-count=3 --verbose >>"${REPORT}" 2>&1 \
    || log "!! 时间戳抓取失败"
log "判读：Timestamp 行应标注时钟来源（Monotonic 等），确认是否无可靠采集时刻"

section "6. 抓帧（SBS 判读与画质检查）"
GRAB_RES=("3840x1080" "3840x1520" "2560x720")
grabbed_any=0
for res in "${GRAB_RES[@]}"; do
    if ! have_res "${res}"; then
        log "跳过 ${res}：设备档位列表中不存在"
        continue
    fi
    w="${res%x*}"; h="${res#*x}"
    raw="${FRAMES_DIR}/${res}_raw.mjpg"
    run v4l2-ctl -d "${DEV}" "--set-fmt-video=width=${w},height=${h},pixelformat=MJPG"
    log "预热 45 帧丢弃（自动曝光收敛）: ${res}"
    timeout 20 v4l2-ctl -d "${DEV}" --stream-mmap=4 --stream-count=45 --stream-to=/dev/null >>"${REPORT}" 2>&1
    log "抓取 10 帧 -> ${raw}"
    timeout 20 v4l2-ctl -d "${DEV}" --stream-mmap=4 --stream-count=10 --stream-to="${raw}" >>"${REPORT}" 2>&1
    rc=$?
    if [ "${rc}" -ne 0 ]; then
        log "!! ${res} 抓帧失败 rc=${rc}"
        continue
    fi
    grabbed_any=1
    # 拆分 MJPG 拼接流为单帧 JPEG：优先 python3，其次 ffmpeg；失败保留拼接文件
    if command -v python3 >/dev/null 2>&1; then
        python3 - "${raw}" "${FRAMES_DIR}/${res}" >>"${REPORT}" 2>&1 <<'PY'
import sys
src, prefix = sys.argv[1], sys.argv[2]
data = open(src, 'rb').read()
frames, start = [], data.find(b'\xff\xd8')
while start != -1:
    end = data.find(b'\xff\xd9', start)
    if end == -1:
        break
    frames.append(data[start:end + 2])
    start = data.find(b'\xff\xd8', end + 2)
for i, f in enumerate(frames):
    with open(f"{prefix}_{i:02d}.jpg", 'wb') as fp:
        fp.write(f)
print(f"split {len(frames)} frames")
PY
        rc=$?
        if [ "${rc}" -eq 0 ]; then
            log "python3 拆分完成: ${FRAMES_DIR}/${res}_NN.jpg"
            rm -f "${raw}"
        else
            log "!! python3 拆分失败 rc=${rc}，保留拼接文件 ${raw}"
        fi
    elif command -v ffmpeg >/dev/null 2>&1; then
        log "\$ ffmpeg -i ${raw} ${FRAMES_DIR}/${res}_%02d.jpg"
        ffmpeg -hide_banner -loglevel error -y -i "${raw}" "${FRAMES_DIR}/${res}_%02d.jpg" >>"${REPORT}" 2>&1
        rc=$?
        if [ "${rc}" -eq 0 ]; then
            log "ffmpeg 拆分完成: ${FRAMES_DIR}/${res}_%02d.jpg"
            rm -f "${raw}"
        else
            log "!! ffmpeg 拆分失败 rc=${rc}，保留拼接文件 ${raw}"
        fi
    else
        log "!! 无 python3/ffmpeg，保留拼接文件 ${raw}（可在 Windows 端用 ffmpeg 拆分）"
    fi
done
[ "${grabbed_any}" -eq 0 ] && log "!! 未抓到任何帧"

section "7. 完成"
log "报告与抓帧目录: ${OUT_DIR}"
log "请将整个目录拷回 Windows（report.txt + frames/*.jpg）交给分析会话"
````

- [ ] **Step 2: bash 语法检查**

Run（Git Bash）: `bash -n tools/stereo_camera_probe.sh && echo SYNTAX_OK`
Expected: 输出 `SYNTAX_OK`，无报错。

- [ ] **Step 3: 内嵌 MJPG 拆分 python 片段的本地夹具验证**

用合成 MJPG 流（两帧假 JPEG 标记）验证拆分逻辑输出 2 个文件，避免板上往返才发现 bug：

Run（Git Bash）:
```bash
wsl.exe bash -c "cd /tmp && printf '\xff\xd8FAKE1\xff\xd9\xff\xd8FAKE2\xff\xd9' > t.mjpg && python3 - t.mjpg /tmp/t_out <<'PY'
import sys
src, prefix = sys.argv[1], sys.argv[2]
data = open(src, 'rb').read()
frames, start = [], data.find(b'\xff\xd8')
while start != -1:
    end = data.find(b'\xff\xd9', start)
    if end == -1:
        break
    frames.append(data[start:end + 2])
    start = data.find(b'\xff\xd8', end + 2)
for i, f in enumerate(frames):
    with open(f'{prefix}_{i:02d}.jpg', 'wb') as fp:
        fp.write(f)
print(f'split {len(frames)} frames')
PY
ls /tmp/t_out_*.jpg && rm -f /tmp/t.mjpg /tmp/t_out_*.jpg"
```
Expected: 输出 `split 2 frames`，随后列出 `/tmp/t_out_00.jpg /tmp/t_out_01.jpg`。

- [ ] **Step 4: 行尾检查**

Run: `grep -c $'\r' tools/stereo_camera_probe.sh || echo LF_OK`
Expected: 输出 `LF_OK`（文件无 CR 字符）。

- [ ] **Step 5: 提交**

```bash
git add tools/stereo_camera_probe.sh
git commit -m "$(cat <<'EOF'
新增双目相机板上 v4l2 探测脚本

用于《双目深度估计思路.md》P3/P4 硬件事实确认：枚举采集节点、
对拍格式档位、逐档实测帧率、确认时间戳来源、抓帧存图，
输出报告目录供回填设计文档。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: 事实确认记录模板落盘

**Files:**
- Create: `docs/双目硬件事实确认记录.md`

**Interfaces:**
- Consumes: 无（独立模板）。
- Produces: `docs/双目硬件事实确认记录.md`，§1~§5 由 Task 4 填写，§6 厂商问题清单由用户发出并填答复，§7 回填勾选由 Task 5/6 完成。

- [ ] **Step 1: 创建模板文件**

将以下内容**逐字**写入 `docs/双目硬件事实确认记录.md`（与 spec §6 一致）：

````markdown
# 双目硬件事实确认记录

> 用途：记录 23H1328889HV1 双目模组（USB UVC）的规格书事实、板上实证结果与厂商答复，
> 作为回填《双目深度估计思路.md》P1~P4 的输入。
> 关联：`docs/superpowers/specs/2026-09-21-双目硬件事实确认-design.md`（本轮设计）。
> 状态：待板上实证 / 待厂商答复。

## 0. 操作步骤

1. 相机插入香橙派 USB 口（免驱 UVC）；确认无其他进程占用相机节点；
2. 板上执行 `bash tools/stereo_camera_probe.sh`；
3. 把生成的 `stereo_probe_YYYYMMDD_HHMMSS/` 目录拷回 Windows；
4. report.txt 与 frames/ 交给分析会话填写本文件 §1~§5；
5. §6 厂商问题清单并行发给厂商，答复填回。

## 1. 设备身份

- 规格书文件名型号 23H1628889HV1 / 正文型号 23H1328889HV1 → 待厂商核实（Q1）
- 厂商：深圳市鑫辉鸿科技有限公司；模组尺寸 80mm×22mm

## 2. P1 焦距 / FOV / 传感器

| 项 | 规格书值 | 实测/答复 | 结论 |
|---|---|---|---|
| 镜头焦距 | EFL 3.0mm，F/NO 2.2，FF/NF | （待填） | |
| FOV | DFOV 92° | （待填，Q4） | 与 EFL/像元不自洽，真实 F 以标定为准 |
| 传感器 | 1/3" CMOS，2µm，72dB | — | |
| 快门 | 电子卷帘快门 | — | 运动形变风险，验证阶段观察 |

- F 初值（标定前占位）：F ≈ 1500px（EFL/像元），仅供误差表趋势参考，不得用于实飞。

## 3. P2 出厂标定

- 规格书：无内参/畸变/外参。厂商答复（Q2）：（待填）
- 若无 → v1.0 前置自标定（棋盘格），与设计文档 §3.1 一致。

## 4. P3 输出形态

- 规格书"同步同帧"、最大 3840×1520 → 倾向 SBS 同帧单流
- 板上判据一（采集节点数）：（待填）
- 板上判据二（抓帧左右半幅判读）：（待填）
- 同步机制厂商答复（Q5）：（待填）
- 结论：（待填：SBS 单流 / 双流）

## 5. P4 MJPG 细节与实测帧率

- 格式对拍（规格书档位 vs --list-formats-ext）：（待填）
- 实测帧率表：档位 | 标称 fps | 实测 fps | 是否掉帧（逐行待填）
- buffer 时间戳来源：（待填，预期无可靠采集时刻，印证设计文档 §3.3）

## 6. 厂商问题清单

| 编号 | 问题 | 答复 | 状态 |
|---|---|---|---|
| Q1 | 型号以 23H1328889HV1 还是 23H1628889HV1 为准？ | | 待问 |
| Q2 | 是否提供出厂标定（内参/畸变/左右目旋转平移）？什么格式？ | | 待问 |
| Q3 | 精确基线（左右光心距）多少？60mm 是否准确？ | | 待问 |
| Q4 | DFOV 92° 与 EFL 3.0mm/2µm 像元不自洽，以哪个为准？92° 对应哪个分辨率档？ | | 待问 |
| Q5 | "同步同帧"的实现机制？左右目曝光是否严格同时？ | | 待问 |

## 7. 结论与回填状态

- P1~P4 最终状态：（待填）
- 回填勾选：双目深度估计思路.md §2 / §3.2 / §5.3 / §15；开发进度.md 双目行
````

- [ ] **Step 2: 行尾检查**

Run: `grep -c $'\r' docs/双目硬件事实确认记录.md || echo LF_OK`
Expected: 输出 `LF_OK`。

- [ ] **Step 3: 提交**

```bash
git add docs/双目硬件事实确认记录.md
git commit -m "$(cat <<'EOF'
新增双目硬件事实确认记录模板

含操作步骤、P1~P4 记录表与厂商问题清单（Q1~Q5），
供板上探测结果与厂商答复回填。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: 板上探测（用户执行门，非编码任务）

**Files:**
- 无仓库文件变更；产物为板上的 `stereo_probe_YYYYMMDD_HHMMSS/` 目录。

**Interfaces:**
- Consumes: Task 1 的 `tools/stereo_camera_probe.sh`。
- Produces: 拷回 Windows 的 `stereo_probe_YYYYMMDD_HHMMSS/`（含 `report.txt` 与 `frames/*.jpg`），其路径告知 Task 4。

- [ ] **Step 1: 相机插入香橙派 USB 口**（确认无其他进程占用相机，例如正式程序未绑定该节点——当前正式程序走 RTSP，无冲突）。

- [ ] **Step 2: 把脚本同步到板上**（二选一：板上 `git pull` 最新 `biao_dev`；或 Windows 端 `scp tools/stereo_camera_probe.sh orangepi@<板IP>:~/`）。

- [ ] **Step 3: 板上确认依赖**：`command -v v4l2-ctl || sudo apt update && sudo apt install -y v4l-utils`。

- [ ] **Step 4: 运行脚本**：`bash stereo_camera_probe.sh`（或在仓库目录 `bash tools/stereo_camera_probe.sh`）。预计 3~5 分钟（各档 300 帧实测 + 预热抓帧）。

- [ ] **Step 5: 把输出目录拷回 Windows**（如 `scp -r orangepi@<板IP>:~/stereo_probe_* ./`），把本地路径与 `report.txt` 内容交给分析会话。

- [ ] **Step 6: 把模板 §6 厂商问题清单（Q1~Q5）发给厂商**（微信/邮件），答复回来后补入。

---

### Task 4: 分析报告并填写记录模板

**Files:**
- Modify: `docs/双目硬件事实确认记录.md`（§1~§5 填实测，§6 填厂商答复）

**Interfaces:**
- Consumes: Task 3 拷回的 `stereo_probe_YYYYMMDD_HHMMSS/report.txt` 与 `frames/*.jpg`。
- Produces: 填好的记录模板，作为 Task 5/6 回填的唯一事实来源。

- [ ] **Step 1: 读报告 §1**（设备枚举）：采集节点数 → 填模板 §4"判据一"。1 个节点 = SBS 单流证据；2 个 = 疑似双流。

- [ ] **Step 2: 读抓帧判读 SBS**（判据二）：用 Read 工具查看 `frames/3840x1080_0*.jpg` 至少 2 张，确认左半幅与右半幅是**同一场景的左右视角**（存在水平视差、内容相似）。同时检查曝光/画质是否可用。结论填模板 §4。

- [ ] **Step 3: 读报告 §3**（格式对拍）：把 `--list-formats-ext` 的 MJPG 档位与规格书 11 档逐一比对，差异（多/少/帧间隔不同）记入模板 §5。

- [ ] **Step 4: 读报告 §4**（实测帧率）：为每档提取 v4l2-ctl 自报 fps，按"档位 | 标称 fps | 实测 fps | 是否掉帧"填模板 §5；重点关注 3840×1520 与 3840×1080 是否真到 30。

- [ ] **Step 5: 读报告 §5**（时间戳）：记录 Timestamp 行的时钟来源标注，填模板 §5 末行。

- [ ] **Step 6: 填写 §1~§3**：设备身份按报告 §0/§2（驱动名、总线）补充；P2/P3 厂商答复已回则一并填入，未回保持"待问"。

- [ ] **Step 7: 提交**

```bash
git add docs/双目硬件事实确认记录.md
git commit -m "$(cat <<'EOF'
回填双目硬件事实确认记录（板上实证）

依据香橙派探测报告与抓帧填写 P1~P4：采集节点数、SBS 判读、
格式对拍、各档实测帧率、buffer 时间戳来源。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: 回填《双目深度估计思路.md》

**Files:**
- Modify: `docs/双目深度估计思路.md`（§2 表、§3.2、§5.3 表、§15 清单）

**Interfaces:**
- Consumes: Task 4 填好的记录模板。
- Produces: 更新后的设计文档；§5.3 误差表新增 F=1500px 档。

- [ ] **Step 1: 更新 §2 硬件表**（依据记录模板实测值）：

- "输出分辨率/帧率"行改为：`SBS 3840×1080 @ 30fps（单目 1920×1080），MJPG 编码`，状态"已确认（规格书 + 板上实证）"；若板上实测该档达不到 30fps，把实测帧率写入并保留 ⚠；
- "镜头焦距 / FOV / 传感器规格"行改为：`EFL 3.0mm（名义）/ DFOV 92°（与 EFL、2µm 像元不自洽，见记录模板 §2）/ 1/3" CMOS / 电子卷帘快门`，状态"名义值已得（规格书）；真实 F 待棋盘格标定"；
- "出厂标定参数"行：按厂商 Q2 答复填"有（格式：___）"或"无 → v1.0 前置自标定"；
- "左右同步形态"行：按板上结论填"SBS 同帧单流（规格书'同步同帧' + 板上实证）"或双流结论；
- 新增一行"快门类型 | 电子卷帘快门 | 已确认（规格书）；运动形变风险列入 §15"。

- [ ] **Step 2: 更新 §3.2**：若板上确认为 SBS 单流，在表格后补一句"板上实证（YYYY-MM-DD）：单采集节点 + 抓帧左右半幅同场景，确认 SBS 同帧单流形态，P3 关闭"；若为双流，改写该节处理分支并与用户讨论后再改 §7 流水线。

- [ ] **Step 3: §5.3 误差表新增 F=1500px 档**（F·B = 1500×0.06 = 90，δD=1px；标注"标定前占位，标定后必须重算"）：

| 距离 Z | 视差 D @ F=1500px | δZ |
|-------:|------:|------:|
| 5 m | 18.0 px | 0.28 m |
| 10 m | 9.0 px | 1.1 m |
| 15 m | 6.0 px | 2.5 m |
| 20 m | 4.5 px | 4.4 m |
| 25 m | 3.6 px | 6.9 m |
| 30 m | 3.0 px | 10.0 m |

并更新 §5.4 有效域表述：按既有判据（D ≥ 3px 且 δZ ≤ 2m），F=1500px 档有效域约 ≤13m（标定前占位）。

- [ ] **Step 4: 更新 §15 待确认清单**：P1 改为"名义值已得（EFL 3.0mm，规格书）；真实 F 待棋盘格标定"；P2/P3 按实际结论更新；P4 改"已实测（YYYY-MM-DD 探测报告）"；新增一行"P9 卷帘快门在机身振动/高速目标下对检测框边缘噪声的影响 | 验证阶段量化 | §4.4 δD 构成"。

- [ ] **Step 5: 提交**

```bash
git add docs/双目深度估计思路.md
git commit -m "$(cat <<'EOF'
回填双目设计文档硬件事实（P1~P4）

规格书 + 板上实证落盘：SBS 3840×1080@30 确认、EFL/FOV 名义值、
卷帘快门风险、§5.3 新增 F=1500px 占位误差表（有效域约 ≤13m）、
§15 清单状态更新。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: 更新《开发进度.md》

**Files:**
- Modify: `docs/开发进度.md`（主链路完成度表双目行、"后续主任务"第 6 条）

**Interfaces:**
- Consumes: Task 5 的回填结论。
- Produces: 进度文档与最新硬件事实一致。

- [ ] **Step 1: 更新主链路完成度表双目行**：

旧：`| 双目深度 | 等待品牌、型号、接口、同步、标定及ARM64 SDK资料，当前阻塞 |`
新：`| 双目深度 | 模组到货（23H1328889HV1，USB UVC），规格书+板上实证已完成（SBS 3840×1080@30）；待棋盘格标定真实 F 与厂商标定答复，随后进入 v1.0 实现 |`

- [ ] **Step 2: 更新"后续主任务"第 6 条**：将"双目和PX4硬件到位前不实现双目融合或正式控制"改为"双目已到货并完成事实确认；v1.0 实现按《双目深度估计思路.md》§12 排期，正式控制仍待 PX4 到位"。

- [ ] **Step 3: 更新文档头"最后更新"日期**为实际回填日期。

- [ ] **Step 4: 提交**

```bash
git add docs/开发进度.md
git commit -m "$(cat <<'EOF'
更新开发进度：双目硬件事实确认完成

双目模组到货且规格书+板上实证完成，解除"等待资料"阻塞；
后续按设计文档 §12 进入 v1.0 实现排期。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## 依赖关系与执行说明

- Task 1 → Task 2 可顺序执行；**Task 3 是用户执行门**（需插相机上板），执行会话在 Task 2 完成后必须停下，把板上操作步骤明确交给用户；
- Task 4 依赖 Task 3 的报告与抓帧；**Task 4 完成后先把填好的记录模板给用户逐条核对，确认后再进 Task 5**（spec §7 要求）；
- Task 5/6 依赖 Task 4；厂商答复（Q1~Q5）可能滞后——滞后项在记录模板与设计文档中保持"待问/待标定"状态，不阻塞其余回填；
- 全部任务不改 `src/`、`include/`、`config/`，无需编译与 `ctest`；每个 Task 结束各自提交。
