# 状态机设计

## 1. 设计目标

状态机负责任务阶段切换和控制权仲裁，不直接实现 YOLO、滤波、避障算法或 MAVLink 编解码。具体策略通过接口注入，便于后续升级摄像头、避障传感器、返航算法和拦截设备。

任务状态、飞行状态、设备健康状态和拦截设备状态必须分别保存，不能用一个枚举表达全部系统状态。

控制权优先级为：

```text
PX4 内部安全保护
  > 遥控器人工接管
  > 算力板安全控制
  > 算力板正常任务控制
  > 地面站任务指令
```

## 2. 任务状态

| 编号 | 状态 | 说明 |
|------|------|------|
| S0 | BOOT | 进程启动和基础资源初始化 |
| S1 | SELF_CHECK | 检查飞控、地面站、摄像头、雷达、模型和配置 |
| S2 | READY | 等待地面站下发任务 |
| S3 | ARMING | 算力板请求 PX4 解锁并验证真实解锁状态 |
| S4 | TAKEOFF | 算力板控制起飞到任务高度 |
| S5 | GPS_APPROACH | 根据地面站目标位置执行远距离位置引导 |
| S6 | VISUAL_HANDOVER | 验证视觉稳定性并完成位置到速度控制切换 |
| S7 | VISUAL_TRACKING | 根据视觉、光流和飞行状态执行速度与方向跟踪 |
| S8 | OBSTACLE_HOLD | V1 检测到前方危险后减速、刹停并悬停 |
| S9 | INTERCEPT_READY | 检查锁定状态、执行机构状态和可选授权 |
| S10 | INTERCEPT_EXECUTING | 调用抽象拦截接口并等待结果 |
| S11 | POST_INTERCEPT | 评估目标和系统状态；V1 默认进入返航 |
| S12 | RETURN_HOME | 算力板控制返回 PX4 上报的 Home 点 |
| S13 | LANDING | 降落并确认落地 |
| S14 | DISARMED | 确认上锁，任务结束，不自动接受第二次任务 |
| S15 | MANUAL_OVERRIDE | 遥控器人工接管 |
| S16 | FAILSAFE | 算力板仍可用时执行降级；完全失效时依赖 PX4 保护 |

## 3. 状态动作表

| 状态 | 入口动作 | 状态内动作 | 出口动作 | 超时或异常处理 |
|------|----------|------------|----------|----------------|
| BOOT | 加载配置、日志和基础资源，禁止控制输出 | 初始化队列、内存池和设备适配器 | 发布初始化结果 | 可恢复错误限次重试；不可恢复错误停止启动 |
| SELF_CHECK | 清空旧健康结果并开始检查 | 汇总 PX4、Home、导航、遥控器、感知和电源能力 | 冻结本次自检结果并上报 | 必需项失败时保持禁止解锁，重试或停止启动 |
| READY | 上报待命和能力清单 | 接收并校验任务、目标和人工指令 | 锁定本次任务参数 | 目标过期时继续待命，不进入解锁 |
| ARMING | 向 PX4 提交解锁请求 | 读取 ACK 和真实 armed 状态 | 记录解锁结果，停止重试计时器 | 确认未解锁则返回 READY；状态不明或链路异常进入 FAILSAFE |
| TAKEOFF | 初始化起飞控制并持续发送有效设定值 | 检查高度、姿态、位置和动力电池 | 清理起飞控制器，初始化接近控制 | 超时或关键状态异常进入 FAILSAFE |
| GPS_APPROACH | 初始化全球/局部位置引导 | 消费最新目标状态并更新位置设定值，同时运行感知 | 停止位置控制输出并保存最后位置目标 | 目标暂时过期先保持；策略超时后返回 Home |
| VISUAL_HANDOVER | 冻结最后位置目标，初始化视觉控制器 | 验证视觉稳定、位置误差、速度和首条限幅指令 | 清除旧位置控制意图或撤销本次切换 | 任一条件失效返回 GPS_APPROACH |
| VISUAL_TRACKING | 启用视觉速度和方向控制 | 融合检测、光流、姿态及有效雷达距离，更新目标估计 | 清除视觉速度意图并保存目标估计 | 目标丢失时执行目标丢失策略，禁止无限预测 |
| OBSTACLE_HOLD | 限速、刹停并记录来源状态 | 保持悬停，持续检查障碍、传感器和人工指令 | 清除制动意图，重新初始化目标状态的控制器 | 危险持续时保持悬停并告警，不自动绕行 |
| INTERCEPT_READY | 冻结锁定快照并检查执行条件 | 检查设备就绪、飞行稳定和可选地面站授权 | 记录授权结果并解除快照锁定 | 授权超时或条件失效时取消执行 |
| INTERCEPT_EXECUTING | 调用通用拦截设备接口 | 监视设备状态、目标状态和中止条件 | 记录执行结果，禁止重复触发 | 超时或设备故障按失败结果进入 POST_INTERCEPT |
| POST_INTERCEPT | 汇总目标、设备、电源和任务结果 | 执行拦截后决策策略 | 固化决策和任务结果 | V1 无法确定时采用默认返回 Home |
| RETURN_HOME | 校验 PX4 Home 并初始化算力板返航控制 | 持续生成返航设定值并运行前向感知 | 清除返航控制意图并初始化降落 | 遇障进入 OBSTACLE_HOLD；控制链失效依赖 PX4 后备保护 |
| LANDING | 初始化降落控制 | 检查高度、垂直速度和 landed 状态 | 请求上锁并确认 disarmed | 超时或状态异常进入 FAILSAFE 或等待人工接管 |
| DISARMED | 上报任务结束并固化结果 | 保持控制输出禁止，允许读取日志和状态 | V1 无自动出口 | 不自动接受第二次任务 |
| MANUAL_OVERRIDE | 立即停止正常任务控制输出 | 监控 PX4、遥控器和系统健康，不自动夺回控制权 | 清除旧任务控制器并重新执行恢复检查 | 接管持续时保持本状态；链路异常由 PX4 保护 |
| FAILSAFE | 记录故障、禁止不安全任务动作 | 链路仍可用时执行悬停、受控返航或降落 | 清除故障控制意图并重新验证系统 | 算力板或链路完全失效时由 PX4 执行后备保护 |

## 4. 状态转移表

### 4.1 正常任务转移

| 当前状态 | 触发事件 | 守卫条件 | 转移动作 | 下一状态 |
|----------|----------|----------|----------|----------|
| BOOT | `EV_INIT_COMPLETE` | 所有基础资源初始化成功 | 发布初始化完成事件 | SELF_CHECK |
| BOOT | `EV_INIT_FAILED` | 错误可恢复且未超过重试次数 | 释放本次资源并重新初始化 | BOOT |
| BOOT | `EV_INIT_FAILED` | 错误不可恢复或重试耗尽 | 保持控制禁止、记录错误并停止进程 | 进程终止（非飞行状态） |
| SELF_CHECK | `EV_SELF_CHECK_UPDATED` | 所有飞行必需项通过 | 上报自检通过和能力清单 | READY |
| SELF_CHECK | `EV_SELF_CHECK_UPDATED` | 存在未通过的飞行必需项且允许重试 | 保持禁止解锁并重新检查失败项 | SELF_CHECK |
| SELF_CHECK | `EV_SELF_CHECK_FAILED` | 必需项不可恢复 | 上报启动失败并停止任务处理 | SELF_CHECK（锁定） |
| READY | `EV_MISSION_START` | 任务、目标、导航、Home、电源和遥控器状态有效 | 保存任务快照并提交解锁请求 | ARMING |
| READY | `EV_MISSION_START` | 任一解锁前置条件不满足 | 拒绝任务并上报具体原因 | READY |
| ARMING | `EV_PX4_STATE_UPDATED` | PX4 确认 `armed == true` 且模式状态符合要求 | 启动起飞设定值流 | TAKEOFF |
| ARMING | `EV_ARM_REJECTED_OR_TIMEOUT` | PX4 确认 `armed == false` | 停止重试并上报失败 | READY |
| TAKEOFF | `EV_FLIGHT_STATE_UPDATED` | 到达配置高度且位置、姿态稳定 | 初始化目标位置引导 | GPS_APPROACH |
| GPS_APPROACH | `EV_VISION_UPDATED` | 目标连续稳定识别且所有切换前置条件满足 | 冻结最后位置目标并初始化视觉控制器 | VISUAL_HANDOVER |
| VISUAL_HANDOVER | `EV_CONTROL_TICK` | 已进入最后位置目标误差范围、速度低于阈值且首条视觉指令有效 | 清除旧位置意图并启用限幅视觉速度意图 | VISUAL_TRACKING |
| VISUAL_HANDOVER | `EV_HANDOVER_CONDITION_LOST` | 任一切换条件失效 | 撤销视觉控制器并恢复最新位置引导 | GPS_APPROACH |
| VISUAL_TRACKING | `EV_INTERCEPT_CONDITION_MET` | 目标稳定锁定、飞行稳定且执行设备可用 | 保存锁定快照并启动授权策略 | INTERCEPT_READY |
| INTERCEPT_READY | `EV_AUTHORIZATION_RESULT` | 自动授权通过或收到有效地面站授权，且执行条件仍成立 | 调用设备准备流程 | INTERCEPT_EXECUTING |
| INTERCEPT_READY | `EV_LOCK_LOST` | 目标不再满足拦截条件但仍可跟踪 | 取消授权和设备准备 | VISUAL_TRACKING |
| INTERCEPT_READY | `EV_AUTHORIZATION_RESULT` | 授权拒绝或超时 | 取消设备准备并记录结果 | RETURN_HOME |
| INTERCEPT_EXECUTING | `EV_INTERCEPTION_FINISHED` | 执行成功或失败结果有效 | 固化设备结果和目标快照 | POST_INTERCEPT |
| INTERCEPT_EXECUTING | `EV_INTERCEPTION_TIMEOUT` | 执行超时或设备故障 | 请求设备安全终止并记录失败 | POST_INTERCEPT |
| POST_INTERCEPT | `EV_POST_POLICY_RESULT` | V1 默认策略，或无目标、能力不足、状态不确定 | 初始化正常返航策略 | RETURN_HOME |
| POST_INTERCEPT | `EV_POST_POLICY_RESULT` | 后续策略允许继续且视觉目标仍稳定 | 重新初始化视觉跟踪器 | VISUAL_HANDOVER |
| POST_INTERCEPT | `EV_POST_POLICY_RESULT` | 后续策略允许继续但只剩有效 GPS 目标 | 初始化目标位置引导 | GPS_APPROACH |
| RETURN_HOME | `EV_HOME_REACHED` | 进入 Home 允许范围且满足降落条件 | 初始化降落控制器 | LANDING |
| LANDING | `EV_PX4_STATE_UPDATED` | PX4 确认 landed | 请求上锁并继续检查状态 | LANDING |
| LANDING | `EV_PX4_STATE_UPDATED` | PX4 确认 landed 且 `armed == false` | 上报任务结束并禁止任务控制 | DISARMED |
| DISARMED | `EV_MISSION_START` | V1 不允许同次上电执行第二次任务 | 拒绝任务并说明需要显式复位 | DISARMED |

### 4.2 目标丢失和GPS目标更新

| 当前状态 | 触发事件 | 守卫条件 | 转移动作 | 下一状态 |
|----------|----------|----------|----------|----------|
| GPS_APPROACH | `EV_TARGET_POSITION_STALE` | 仅短时过期且策略允许等待 | 发送悬停意图并等待新目标状态 | GPS_APPROACH |
| GPS_APPROACH | `EV_TARGET_POSITION_STALE` | 超过策略时限或目标无效 | 取消目标位置引导并初始化返航 | RETURN_HOME |
| VISUAL_TRACKING | `EV_VISUAL_TARGET_LOST` | 预测仍在上限内且最新 GPS 目标仍在附近 | 限制速度并按预测方向转动整机搜索 | VISUAL_TRACKING |
| VISUAL_TRACKING | `EV_VISUAL_TARGET_LOST` | 最新 GPS 目标有效且已明显离开视觉搜索区域 | 清除视觉速度意图并初始化位置引导 | GPS_APPROACH |
| VISUAL_TRACKING | `EV_VISUAL_TARGET_LOST` | 预测、搜索和地面站目标均无效 | 停止目标追踪并初始化返航 | RETURN_HOME |
| VISUAL_TRACKING | `EV_VISION_REACQUIRED` | 目标编号、位置连续性和置信度重新满足要求 | 重置或校正状态估计器 | VISUAL_TRACKING |

视觉丢失的预测上限、GPS“附近”范围、搜索扫描范围和超时仍是配置项。卡尔曼滤波只提供有限时间预测，不单独决定下一状态。

### 4.3 障碍物转移

| 当前状态 | 触发事件 | 守卫条件 | 转移动作 | 下一状态 |
|----------|----------|----------|----------|----------|
| GPS_APPROACH | `EV_OBSTACLE_RISK` | 风险达到制动等级 | 保存来源状态，限速并刹停 | OBSTACLE_HOLD |
| VISUAL_HANDOVER | `EV_OBSTACLE_RISK` | 风险达到制动等级 | 撤销控制切换，限速并刹停 | OBSTACLE_HOLD |
| VISUAL_TRACKING | `EV_OBSTACLE_RISK` | 风险达到制动等级 | 清除视觉速度意图，限速并刹停 | OBSTACLE_HOLD |
| RETURN_HOME | `EV_OBSTACLE_RISK` | 风险达到制动等级 | 暂停返航，限速并刹停 | OBSTACLE_HOLD |
| OBSTACLE_HOLD | `EV_OBSTACLE_CLEARED` | 来源为 GPS_APPROACH，危险解除且目标位置仍有效 | 重新初始化位置控制器 | GPS_APPROACH |
| OBSTACLE_HOLD | `EV_OBSTACLE_CLEARED` | 来源为视觉控制，危险解除且视觉重新稳定 | 重新执行视觉接管检查 | VISUAL_HANDOVER |
| OBSTACLE_HOLD | `EV_OBSTACLE_CLEARED` | 来源为 RETURN_HOME，危险解除且 Home、导航有效 | 重新初始化返航控制器 | RETURN_HOME |
| OBSTACLE_HOLD | `EV_OBSTACLE_PERSISTED` | 超过等待时限 | 保持悬停并请求遥控器或地面站处置 | OBSTACLE_HOLD |

V1 的障碍策略不生成绕行路径。未来策略可以增加新的决策结果，但仍通过统一风险和控制约束接口与状态机交互。

### 4.4 人工接管、返航和故障转移

| 当前状态 | 触发事件 | 守卫条件 | 转移动作 | 下一状态 |
|----------|----------|----------|----------|----------|
| 任意自动飞行状态 | `EV_RC_OVERRIDE` | PX4 确认遥控器接管有效 | 立即停止正常任务控制输出 | MANUAL_OVERRIDE |
| MANUAL_OVERRIDE | `EV_AUTONOMY_RESUME_REQUEST` | 已落地且 PX4 确认 disarmed | 清除旧任务上下文并保持禁止自动起飞 | DISARMED |
| MANUAL_OVERRIDE | `EV_AUTONOMY_RESUME_REQUEST` | 仍在空中且模式、导航、Home 和链路重新验证通过 | 不恢复旧追踪任务，初始化保守返航 | RETURN_HOME |
| ARMING | `EV_RETURN_OR_CANCEL` | PX4 确认未解锁 | 取消解锁和任务 | READY |
| TAKEOFF/GPS_APPROACH/VISUAL_HANDOVER/VISUAL_TRACKING/INTERCEPT_READY | `EV_RETURN_OR_CANCEL` | 飞行器在空中且算力板控制链正常 | 中止当前任务动作并初始化返航 | RETURN_HOME |
| INTERCEPT_EXECUTING | `EV_RETURN_OR_CANCEL` | 设备支持安全中止 | 请求设备安全中止，完成结果记录 | POST_INTERCEPT |
| 任意自动飞行状态 | `EV_CRITICAL_FAILURE` | 算力板仍运行且 PX4 链路可用 | 停止正常任务并执行故障策略 | FAILSAFE |
| FAILSAFE | `EV_FAILURE_RECOVERED` | 飞行器在空中且导航、Home 和控制链重新验证通过 | 不恢复旧任务，初始化保守返航 | RETURN_HOME |
| FAILSAFE | `EV_PX4_STATE_UPDATED` | 已落地且 PX4 确认 disarmed | 固化故障结果 | DISARMED |
| 任意状态 | 算力板断电、进程完全失效或 PX4 串口完全中断 | 状态机无法继续执行 | 不存在算力板转移动作，由 PX4 检测 Offboard 丢失 | PX4 后备保护（状态机外部） |

### 4.5 转移判定规则

同一控制周期只允许提交一个任务状态转移。多个事件同时满足时按以下优先级选择：

```text
PX4 已进入内部保护或算力板失去控制能力
  > 遥控器人工接管
  > 算力板检测到关键故障
  > 障碍物制动
  > 返航或取消
  > 正常任务状态转移
```

状态转移执行顺序固定为：

```text
检查事件和守卫条件
  -> 执行当前状态出口动作
  -> 执行转移动作
  -> 更新当前状态和进入时间
  -> 执行下一状态入口动作
  -> 发布状态转移事件
```

守卫条件必须使用同一控制周期生成的不可变数据快照。状态恢复必须重新验证下一状态入口条件，不能仅凭“恢复到故障前状态”直接跳转。

## 5. 主状态流

```text
BOOT
  -> SELF_CHECK
  -> READY
  -> ARMING
  -> TAKEOFF
  -> GPS_APPROACH
  -> VISUAL_HANDOVER
  -> VISUAL_TRACKING
  -> INTERCEPT_READY
  -> INTERCEPT_EXECUTING
  -> POST_INTERCEPT
  -> RETURN_HOME
  -> LANDING
  -> DISARMED
```

公共转移：

```text
飞行状态 + 遥控器接管       -> MANUAL_OVERRIDE
飞行状态 + 可处理关键异常    -> FAILSAFE
飞行状态 + 算力板完全失效    -> PX4 独立执行后备保护
GPS_APPROACH/VISUAL_TRACKING/RETURN_HOME + 前向危险 -> OBSTACLE_HOLD
```

## 6. 状态详细设计

### S0 BOOT

入口动作：

- 加载配置并初始化日志。
- 初始化消息队列、内存池和各硬件适配器。
- 初始化 H.265 解码、YOLO、光流和状态估计模块。
- 所有控制输出默认禁止。

成功进入 `SELF_CHECK`，基础资源不可恢复失败则停止启动并上报告警。

### S1 SELF_CHECK

检查项：

- PX4 MAVLink 心跳、固件版本、armed 状态和飞行模式。
- PX4 GPS、姿态、航向、动力电池和 Home 点有效性。
- 遥控器可用状态以及接管状态。
- 地面站链路和任务消息解析能力。
- TCP 摄像头连接、H.265 解码和视频帧新鲜度。
- YOLO 模型和光流算法初始化状态。
- 单点激光雷达通信、量程和数据新鲜度。
- 算力板电池监测接口；硬件完成前标记为“能力未提供”，不得伪造正常数据。

自检结果区分必需能力、可降级能力和未安装能力。飞行安全必需项失败时禁止解锁。

### S2 READY

- 上报待命状态和能力清单。
- 接收包含目标完整状态的任务消息。
- 只有任务消息有效、PX4 导航有效、Home 有效且所有解锁条件满足时才进入 `ARMING`。
- 取消和返航命令保留，但 V1 不从 `DISARMED` 自动开始第二次任务。

### S3 ARMING

- 由算力板向 PX4 请求解锁。
- MAVLink ACK 只作为过程反馈，最终以 PX4 上报的 armed 状态为准。
- 超时或拒绝后有限次重试；仍失败则返回 `READY` 并上报告警。
- 遥控器接管立即进入 `MANUAL_OVERRIDE`。

### S4 TAKEOFF

- 算力板按 PX4 1.11.3 的控制契约持续发送有效设定值并控制起飞。
- 持续检查高度、位置、姿态、控制模式和动力电池。
- 达到配置高度且飞行状态稳定后进入 `GPS_APPROACH`。
- 起飞超时、导航失效或控制链路异常进入 `FAILSAFE`。

### S5 GPS_APPROACH

- 地面站持续发送目标编号、经纬度、高度、速度、航向、定位精度、时间戳和有效期。
- 算力板利用 PX4 上报的本机位置计算目标距离和方向，持续更新位置引导设定值。
- 过期或跳变异常的目标位置不得直接用于控制。
- 摄像头、YOLO 和光流持续工作，寻找目标。
- 视觉目标满足稳定锁定条件后进入 `VISUAL_HANDOVER`。
- 前向危险进入 `OBSTACLE_HOLD`。

### S6 VISUAL_HANDOVER

位置控制切换到视觉速度控制必须同时满足：

- 目标连续稳定识别，置信度和检测连续性满足配置。
- 图像、检测和本机姿态均未超龄。
- 无前向危险。
- 飞行器进入上一位置目标的允许误差范围。
- 当前速度低于切换阈值。

第一条视觉速度设定值必须经过限幅，避免位置和速度控制切换产生突变。任一条件失效时返回 `GPS_APPROACH`。

### S7 VISUAL_TRACKING

- 根据目标像素位置、固定相机标定参数、本机姿态和光流估计目标视线及运动趋势。
- 单点雷达只在波束与目标区域满足关联条件时作为目标距离；其他情况下仅作为前向安全距离。
- 状态估计器可使用卡尔曼滤波平滑测量并处理短时丢帧。
- 卡尔曼预测不得无限期生成控制指令，预测时长和不确定度必须有上限。
- 视觉丢失时调用 `ITargetLostStrategy`：结合最新有效的地面站目标位置决定预测、整机转向搜索或退回 `GPS_APPROACH`。
- 视觉丢失原因可能是遮挡、漏检、目标离开或拦截结果，V1 不强行分类。
- 前向危险进入 `OBSTACLE_HOLD`。
- 满足锁定和拦截前置条件后进入 `INTERCEPT_READY`。

### S8 OBSTACLE_HOLD

V1 的 `IObstacleAvoidanceStrategy` 实现为安全停止策略：

1. 根据前向距离和相对运动趋势限速。
2. 进入制动阈值后发送刹停指令。
3. 稳定后保持悬停。
4. 上报障碍物、距离、画面方位和原任务状态。
5. 等待危险解除、地面站指令或遥控器接管。

V1 不自动选择左右绕行方向。危险解除并重新验证数据后，可回到进入本状态前的 `GPS_APPROACH`、`VISUAL_TRACKING` 或 `RETURN_HOME`。恢复不是简单跳回旧状态，必须重新检查对应入口条件。

后续升级只替换避障策略，例如视觉可通行区域、单目深度、扫描测距或路径规划，不修改任务状态机。

### S9 INTERCEPT_READY

- 检查目标锁定质量、相对状态、飞行稳定性和执行机构就绪状态。
- 检查动力电池和算力板电池能力；算力板电池硬件未完成前按照配置采取保守策略。
- `IInterceptionAuthorizationPolicy` 支持自动授权和地面站确认两种实现。
- V1 默认允许采用自动授权，但保留地面站确认模式。
- 条件满足后进入 `INTERCEPT_EXECUTING`；条件失效返回 `VISUAL_TRACKING`。

### S10 INTERCEPT_EXECUTING

- 通过 `IInterceptionDevice` 调用具体执行机构。
- 状态机只关心未提供、未就绪、执行中、成功、失败、已使用和故障等通用状态。
- 具体捕网或其他手段不进入状态机实现。
- 完成或失败后均进入 `POST_INTERCEPT`。

### S11 POST_INTERCEPT

- 记录拦截结果、目标状态、执行机构剩余能力和系统健康。
- `IPostInterceptionPolicy` 保留自动返航、自动继续和地面站决定三种策略。
- V1 默认策略为拦截结束后返回 Home。
- 没有目标、执行机构不可用、电量不足或关键能力异常时必须返回。

### S12 RETURN_HOME

- 使用 PX4 上报且经验证有效的 Home 点；算力板没有独立 GPS。
- 正常情况下由算力板规划返回过程并持续向 PX4 发送控制设定值。
- 摄像头、光流和单点雷达在返航时继续运行。
- V1 遇到障碍物进入 `OBSTACLE_HOLD`，等待人工处置或危险解除，不自动绕行。
- 到达 Home 附近并满足降落条件后进入 `LANDING`。
- 算力板、控制程序或飞控串口完全失效时，无法继续由算力板控制，此时依赖 PX4 配置的 Offboard 丢失保护执行后备 RTL 或其他安全动作。

### S13 LANDING

- 算力板正常时控制降落，持续检查高度、垂直速度和 landed 状态。
- 确认落地后请求上锁。
- 只有 PX4 确认 disarmed 后才能进入 `DISARMED`。

### S14 DISARMED

- 上报任务结束、拦截结果和故障摘要。
- V1 一次上电任务默认只执行一次拦截，不能自动回到 `READY`。
- 如后续支持第二次任务，必须新增显式复位、自检和执行机构恢复流程。

### S15 MANUAL_OVERRIDE

- 检测到遥控器接管后停止正常任务设定值，避免与人工控制冲突。
- 保存接管前状态和原因，但不得未经重新验证直接恢复。
- 恢复自动控制需要明确操作，并重新检查 PX4 模式、位置、Home、目标数据和障碍物状态。

### S16 FAILSAFE

算力板仍在工作且 PX4 链路可用时，可根据错误执行悬停、受控返航或降落。若算力板或链路已经完全失效，安全动作只能由 PX4 自身执行。

精确的 PX4 1.11.3 Offboard 丢失、遥控器失联和 RTL 参数必须在实现前查阅对应版本文档，并通过 SITL、台架和受控实飞确认，不能依赖默认值。

## 7. 可替换策略

| 接口 | V1 实现 | 后续升级方向 |
|------|---------|--------------|
| `IObstacleAvoidanceStrategy` | 减速、刹停、悬停 | 视觉绕行、深度感知、路径规划 |
| `ITargetLostStrategy` | 短时预测、GPS 校验、整机转向搜索 | 云台搜索、多模型跟踪 |
| `IReturnStrategy` | 算力板控制返回 PX4 Home | 带地图或局部规划的返航 |
| `IInterceptionAuthorizationPolicy` | 自动或地面站确认 | 分级授权规则 |
| `IPostInterceptionPolicy` | V1 默认返航 | 自动继续或地面站决策 |
| `IInterceptionDevice` | 通用占位实现 | 捕网或其他设备适配器 |
| `ICompanionPowerMonitor` | 硬件待定 | 电量计、BMS 或 UPS 适配器 |

## 8. 待定参数

以下参数必须配置化，并在仿真和实机测试后确定：

- 视觉稳定锁定帧数、置信度和数据超时。
- 位置到速度控制切换的位置误差和速度阈值。
- 卡尔曼预测上限、搜索时间和 GPS 目标有效期。
- 激光雷达减速、制动、解除阈值及滤波参数。
- 起飞、跟踪、返航的速度、加速度和距离限制。
- 地面站、PX4、摄像头和雷达链路超时。
- PX4 1.11.3 的失联保护和遥控器接管参数。
