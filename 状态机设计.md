# 状态机设计

## 状态列表

| 编号 | 状态 | 说明 |
|------|------|------|
| S0 | 上电自检 | 系统启动，逐一检查各硬件链路是否正常 |
| S1 | 加载模型 | 加载 YOLOv26 模型到内存 |
| S2 | 待命 | 自检通过、模型就绪，等待地面站下发任务指令 |
| S3 | 解锁 | 飞控解锁，电机进入怠速 |
| S4 | 起飞爬升 | 垂直起飞至目标高度 |
| S5 | 地面站引导 | 根据地面站下发的目标位置飞行，激光测距避障 |
| S6 | 视觉跟踪 | 摄像头捕获目标后转入视觉主导跟踪，地面站引导降级为辅助 |
| S7 | 反制（预留） | 锁定目标后执行反制操作 |
| S8 | 任务完成 | 任务成功或失败，返航降落 |
| S9 | 失控保护 | 关键链路异常，执行保护策略（悬停/降落/返航） |

## 状态转移图

```
                    ┌─────────┐
                    │ S0 上电自检 │
                    └────┬────┘
                         │ 自检通过
                    ┌────▼────┐
              ┌─────│ S1 加载模型 │
              │     └────┬────┘
              │          │ 加载完成
              │     ┌────▼────┐
              │     │ S2 待命  │◄──────────────────┐
              │     └────┬────┘                   │
              │          │ 收到起飞指令              │
              │     ┌────▼────┐                   │
              │     │ S3 解锁  │                   │
              │     └────┬────┘                   │
              │          │ 解锁成功                 │
              │     ┌────▼────┐                   │
              │     │S4 起飞爬升│                   │
              │     └────┬────┘                   │
              │          │ 到达目标高度              │
              │     ┌────▼────┐                   │
              │     │S5 地面站引导│                  │
              │     └────┬────┘                   │
              │          │ 摄像头捕获目标             │
              │     ┌────▼────┐                   │
              │     │S6 视觉跟踪 │                  │
              │     └────┬────┘                   │
              │     ┌────┴────┐                  │
              │     │         │                  │
              │  ┌──▼──┐  ┌───▼───┐              │
              │  │S7 反制│  │S8 任务完成│              │
              │  └──┬──┘  └───┬───┘              │
              │     │         │                  │
              │     └────┬────┘                  │
              │          │ 返航降落完成             │
              │          └───────────────────────┘
              │
              │  任意状态 ──(关键链路异常)──► S9 失控保护
              └─────────────────────────────┘
```

## 详细设计

### S0 上电自检

**入口动作**：
- 依次检查各硬件链路通信是否正常
- 检查项：PX4 心跳、地面站心跳、摄像头画面、图传链路、激光测距雷达数据

**自检策略**：
- 各检查项独立进行，记录每项结果
- 全部通过 → 转入 S1
- 部分失败 → 重试 3 次，仍失败则根据失败项决定是否降级运行或转入 S9

**出口判断**：
- 通过 → S1
- 失败（不可恢复）→ S9

### S1 加载模型

**入口动作**：
- 从本地路径加载 YOLOv26 模型权重和配置
- 初始化推理引擎

**出口判断**：
- 加载成功 → S2
- 加载失败（文件缺失/内存不足）→ S9

### S2 待命

**入口动作**：
- 向地面站上报"待命"状态
- 持续监听地面站指令

**状态内循环**：
- 维持 PX4 心跳、地面站心跳
- 接收地面站下发的目标位置、任务参数

**出口判断**：
- 收到起飞指令 → S3
- 收到返航/取消指令 → 保持 S2
- 心跳超时 → S9
- 收到强制降落指令 → S8

### S3 解锁

**入口动作**：
- 发送 MAVLink 解锁指令
- 等待 PX4 返回解锁确认

**出口判断**：
- 解锁成功 → S4
- 解锁失败（超时/拒绝）→ 重试 3 次，仍失败 → S2（上报地面站）
- 链路异常 → S9

### S4 起飞爬升

**入口动作**：
- 发送起飞指令，目标高度从配置读取
- 持续监测当前高度和姿态

**出口判断**：
- 到达目标高度 → S5
- 起飞超时未达标 → S9
- 链路异常 → S9
- 地面站下发返航指令 → S8

### S5 地面站引导

**入口动作**：
- 切换 PX4 为机载模式（GUIDED）
- 启动激光测距雷达避障检测

**状态内行为**：
- 接收地面站下发的目标无人机位置（经纬度、高度）
- 将目标位置封装为 MAVLink 位置指令发给 PX4
- 激光测距雷达数据用于避障：若前方距离小于安全阈值，发送悬停指令并上报地面站
- 同时运行摄像头采集和 YOLOv26 识别，尝试捕获视觉目标

**出口判断**：
- YOLOv26 成功识别到目标无人机 → S6
- 地面站下发返航指令 → S8
- 地面站心跳超时 → 悬停等待 → 超时后 S9
- PX4 链路异常 → S9
- 激光测距雷达异常 → 上报地面站，可继续飞行但失去避障能力，视严重程度可转 S9

### S6 视觉跟踪

**入口动作**：
- 通知地面站进入视觉跟踪模式
- 地面站引导降级为辅助（仅提供大致方位，不作为主要控制源）

**状态内行为**：
- 以 YOLOv26 识别到的目标位置为核心，计算目标相对本机的方位和距离
- 结合激光测距雷达数据修正目标距离
- 轨迹预测模块持续预测目标轨迹
- 生成追踪位置指令发给 PX4

**丢失目标处理**：
- 短暂丢失（< 2秒）：保持当前轨迹预测，继续追踪
- 持续丢失（2~5秒）：悬停，扩大搜索范围（升高/旋转）
- 长时间丢失（> 5秒）：上报地面站，退回 S5 地面站引导模式

**出口判断**：
- 满足反制条件（距离、位置锁定）→ S7
- 目标丢失超时 → S5
- 地面站下发返航指令 → S8
- 链路异常 → S9

### S7 反制（预留）

**入口动作**：
- 锁定目标，进入反制姿态
- 通知地面站进入反制状态

**出口判断**：
- 反制完成 → S8
- 反制失败 → S8
- 链路异常 → S9

### S8 任务完成

**入口动作**：
- 发送返航指令（RTL）或降落指令（LAND）
- 通知地面站任务结束状态

**出口判断**：
- 降落完成 → S2（待命，可接受新任务）
- 链路异常 → S9

### S9 失控保护

**入口动作**：
- 记录触发失控的链路和原因
- 根据配置的策略执行保护动作

**保护策略**（由配置决定）：
- 策略 A（悬停等待）：发送悬停指令，等待链路恢复或超时
- 策略 B（自动返航）：发送 RTL 指令
- 策略 C（原地降落）：发送 LAND 指令
- 策略 D（继续飞行并告警）：维持当前模式，向地面站持续告警

**出口判断**：
- 链路恢复且收到地面站恢复指令 → 回到断链前状态 S2~S6
- 降落完成 → S2

## 关键链路异常检测

| 链路 | 检测方式 | 超时阈值（待定） | 严重程度 |
|------|----------|------------------|----------|
| PX4 串口 | MAVLink 心跳超时 | 2s | 严重 |
| 地面站电台 | MAVLink 心跳超时 | 5s | 中等 |
| 摄像头 | 视频帧超时 | 1s | 低（仅影响视觉跟踪） |
| 图传 | 发送失败累计 | 3 次 | 低 |
| 激光测距雷达 | 数据帧超时 | 500ms | 中等 |

## 地面站指令

| 指令 | 触发条件 | 作用 |
|------|----------|------|
| 起飞 | S2 待命状态 | 进入 S3 解锁流程 |
| 返航 | S3~S8 任意状态 | 转入 S8 任务完成 |
| 强制降落 | 任意状态 | 立即降落 |
| 目标位置 | S5 地面站引导 | 更新目标无人机位置信息 |
| 任务恢复 | S9 失控保护 | 从保护状态恢复 |
